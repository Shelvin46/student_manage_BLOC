import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

TextStyle appbarText = const TextStyle(fontSize: 30);
Padding textPaddingHome = const Padding(
  padding: EdgeInsets.all(20.0),
);
EdgeInsets edgeInsets = const EdgeInsets.all(20.0);
TextStyle homeText1 = const TextStyle(fontSize: 24.0, color: Colors.blue);
Icon iconHome = const Icon(Icons.add);
SizedBox gapinAccount = const SizedBox(
  height: 20,
);
Icon iconofSearch = const Icon(Icons.search);
Icon deleteIcon = const Icon(
  Icons.delete,
  color: Colors.red,
);
